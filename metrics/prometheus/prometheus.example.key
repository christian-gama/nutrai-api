Obtain the secret from the .env file (APP_API_KEY) or run the command `make prometheus-key`
to automatically set it. Remove this comment and leave only the secret as value in case you're
setting it manually.