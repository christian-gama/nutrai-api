{
  // Setup the test environment variables to make it possible to run tests
  // through the VSCode test runner.
  "go.testEnvVars": {
    "TEST_MODE": "all",
    "DB_HOST": "localhost",
    "DB_PORT": 5434,
  },
  "go.testOnSave": true
}